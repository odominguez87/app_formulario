import React from 'react';
import ReactDOM from 'react-dom/client';
import Listado from './componentes/Listado';

const listado = ReactDOM.createRoot(document.getElementById('listado'));
root.render(
  <React.StrictMode>
    <Listado />
  </React.StrictMode>
);